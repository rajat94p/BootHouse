<% @ratings.each do |rating| %>
  <%#= ratings_path(rating) %>
  <%= rating.description%>
  <%= button_to 'like', rating_likes_path(rating_id: rating.id,current_user_id: current_user.id), :method => :post %>
  like_counts :<%= rating.likes.count %><br><br>
<% end %>

<%= link_to 'Back', cars_path%>